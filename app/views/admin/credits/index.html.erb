<% provide :title, t(".title") %>
<div class="row">
  <div class="col-md-12">
    <div class="card">
      <div class="card-header">
        <h4 class="card-title">
          <%= t ".title" %>
          <%= link_to t(".add_new"), new_admin_credit_path, class: "btn btn-primary btn-xs right" %>
        </h4>
      </div>
      <div class="card-body">
        <table class="table table-hover table-striped">
          <thead>
            <th>#</th>
            <th><%= t ".user" %></th>
            <th><%= t ".amount" %></th>
            <th><%= t ".type" %></th>
            <th><%= t ".time" %></th>
            <th><%= t ".action" %></th>
          </thead>
          <tbody>
            <%= render @credits %>
          </tbody>
        </table>
        <%= paginate @credits %>
      </div>
    </div>
  </div>
</div>
